<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.hanju.salling.mapper.SaleMapper">
	<!-- 판매 물품 리스트 -->
	<select id="getProductList" parameterType="hashmap" resultType="com.example.hanju.salling.model.entity.SaleModel">
		SELECT PRODUCT_NAME, TO_CHAR(PRICE, 'FM99,999') AS PRICE_COMMA, ALCOHOL, FILE_ORG_NAME, FILE_PATH
		FROM HANJU_PRODUCT P
		INNER JOIN HANJU_IMAGE I ON P.PRODUCT_ID = I.FK_ID
		WHERE TYPE = 'TRADITION' OR TYPE = 'GIFTSET'
	</select>
	
	<!-- 게시 리스트 페이징 -->
	<select id="pageNum" parameterType="hashmap" resultType="int">
		SELECT COUNT(*) AS COUNT	
		FROM HANJU_PRODUCT
		WHERE TYPE = 'TRADITION' OR TYPE = 'GIFTSET'
	</select>
</mapper>