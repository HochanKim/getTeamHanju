<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.hanju.pickup.mapper.PickupMapper">
	<select id="getBoardList" parameterType="hashmap" resultType="Product">
		SELECT P.PRODUCT_NAME, P.PRICE, P.ALCOHOL, P.COLOR, P.MATERIAL, P.CAPACITY
		FROM TEST_PRODUCT P
		WHERE TYPE = 'WINE' AND P.PRODUCT_NAME LIKE '%'||#{searchKeyword}||'%'
	</select>
</mapper>